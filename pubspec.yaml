name: cropsureconnect
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0' # Updated for broader compatibility

dependencies:
  flutter:
    sdk: flutter

  # --- Existing Dependencies ---
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  get: ^4.6.6
  cupertino_icons: ^1.0.8
  intl: ^0.19.0 # Version adjusted for consistency
  image_picker: ^1.1.2
  file_picker: ^8.0.5
  collection: ^1.18.0
  fl_chart: ^0.68.0
  provider: ^6.1.2

  # --- NEW DEPENDENCIES FOR PAYMENT PAGE ---
  google_fonts: ^6.2.1
  animations: ^2.0.11
  lottie: ^3.1.2
  flutter_slidable: ^3.1.0
  camera: ^0.11.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    # If you have sub-folders for images or Lottie animations, declare them
    # - assets/images/
    # - assets/animations/